1. Accept ``OR`` as the operator for multiple filter expressions
2. Return objects instead of their IDs for back references (``Group.members``, ``Person.groups``)
3. Allow editing and deleting entities
